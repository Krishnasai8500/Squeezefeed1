def generate_overlay_visual(prompt: str):

    # Temporary low-cost fallback
    # Later replace with:
    # source image + typography overlay + premium design

    return f"overlay_visual_for_{abs(hash(prompt))}.png"